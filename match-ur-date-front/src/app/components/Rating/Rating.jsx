import React from 'react';
import { Typography } from '@mui/material';
import styles from './Rating.styles';

const Initial = () => {
    return (
            <Typography sx={styles.info}>Tela de avaliação</Typography>
    );
};

export default Initial;
