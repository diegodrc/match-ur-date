import React from 'react';
import {
    Card
} from '@mui/material';

const Initial = (props) => {
    // ({ setScreen }) = props;

    return (
        <Card sx={{ p: '10%' }}><span>teste</span></Card>
    );
};

export default Initial;
